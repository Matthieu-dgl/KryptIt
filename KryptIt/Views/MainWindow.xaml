<Window x:Class="KryptIt.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:KryptIt.ViewModels"
        xmlns:conv="clr-namespace:KryptIt.Converter"
        xmlns:views="clr-namespace:KryptIt.Views"
        Title="KryptIt" Width="1000" Height="600"
        Background="#F7F7F7"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Converters -->
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <!-- Styles pour Sidebar et Header -->
        <Style x:Key="SidebarItemTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="10,5,0,5"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Styles généraux -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12 6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1"
                                CornerRadius="15"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernListView" TargetType="ListView">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
        <Style x:Key="WatermarkTextBox" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock Text="Search"
                                       Foreground="Gray"
                                       Margin="5,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Lier le DataContext au MainViewModel -->
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="White" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="Lock" Width="24" Height="24" Foreground="Black" Margin="0,0,5,0"/>
                    <TextBlock Text="KryptIt" Style="{StaticResource HeaderTextStyle}" />
                </StackPanel>

                <StackPanel Grid.Column="1" />

                <Button Grid.Column="2"
                        Command="{Binding OpenPopupCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Margin="5,0,0,0"
                        VerticalAlignment="Center">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="Plus" Width="20" Height="20" Margin="0,0,10,0"/>
                            <TextBlock Text="Ajouter" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button.Content>
                </Button>

                <materialDesign:PackIcon Kind="Account" Width="24" Height="24" Foreground="Black" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="3" Margin="20,0,10,0"/>
            </Grid>
        </Border>

        <!-- CONTENU PRINCIPAL -->
        <Grid Grid.Row="1">
            <!-- Vue par défaut : Sidebar + MainContent -->
            <Grid x:Name="DefaultContent"
                  Visibility="{Binding IsDefaultViewVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0">
                            <TextBlock Text="Tags" Margin="10,15,0,5" FontWeight="Bold" FontSize="14" Foreground="Black"/>
                            <StackPanel Margin="10,0,0,0">
                                <TextBlock Text="Work" Style="{StaticResource SidebarItemTextStyle}"/>
                                <TextBlock Text="Personal" Style="{StaticResource SidebarItemTextStyle}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Bouton Settings en bas -->
                        <Button Grid.Row="2"
                                Command="{Binding OpenSettingsCommand}"
                                Style="{StaticResource PrimaryButton}"
                                Margin="10,10,10,20"
                                VerticalAlignment="Bottom">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <materialDesign:PackIcon Kind="Settings" Width="20" Height="20" Margin="0,0,10,0"/>
                                    <TextBlock Text="Settings" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </Grid>
                </Border>

                <!-- MainContent : Recherche et Tableau -->
                <Grid Grid.Column="1" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Zone de Recherche -->
                    <Border Grid.Row="0" Height="40" Background="White" CornerRadius="5" Margin="0 0 0 10" BorderThickness="1" BorderBrush="#E0E0E0">
                        <Grid>
                            <!-- Icône de loupe -->
                            <materialDesign:PackIcon Kind="Magnify"
                                 Width="16"
                                 Height="16"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Center"
                                 Margin="10,0,0,0" />

                            <!-- La TextBox -->
                            <TextBox Style="{StaticResource WatermarkTextBox}" 
                                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                FontSize="14"
                                VerticalContentAlignment="Center"
                                VerticalAlignment="Center"
                                Padding="3 0 0 0"
                                Margin="25 0 0 0" />
                        </Grid>
                    </Border>

                    <!-- Tableau des mots de passe -->
                    <Border Grid.Row="1" Background="White" CornerRadius="5" BorderThickness="1" BorderBrush="#E0E0E0">
                        <ListView ItemsSource="{Binding FilteredPasswords}"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  Style="{StaticResource ModernListView}"
                                  SelectedItem="{Binding SelectedPassword}">
                            <ListView.Resources>
                                <ContextMenu x:Key="ItemContextMenu">
                                    <MenuItem Header="Open in new Tab" Command="{Binding DataContext.OpenInNewTabCommand, RelativeSource={RelativeSource AncestorType=ListView}}" />
                                    <Separator/>
                                    <MenuItem Header="Copy Username" Command="{Binding DataContext.CopyUsernameCommand, RelativeSource={RelativeSource AncestorType=ListView}}" />
                                    <MenuItem Header="Copy Password" Command="{Binding DataContext.CopyPasswordCommand, RelativeSource={RelativeSource AncestorType=ListView}}" />
                                    <MenuItem Header="Copy Website" Command="{Binding DataContext.CopyWebsiteCommand, RelativeSource={RelativeSource AncestorType=ListView}}" />
                                    <Separator/>
                                    <MenuItem Header="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListView}}" />
                                </ContextMenu>
                                <ContextMenu x:Key="EmptyAreaContextMenu">
                                    <MenuItem Header="Add an entry" Command="{Binding OpenPopupCommand}" />
                                </ContextMenu>
                            </ListView.Resources>
                            <ListView.ContextMenu>
                                <StaticResource ResourceKey="EmptyAreaContextMenu"/>
                            </ListView.ContextMenu>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.InputBindings>
                                <KeyBinding Key="Delete" Command="{Binding DeleteCommand}" />
                            </ListView.InputBindings>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="URL" Width="180">
                                        <GridViewColumn.HeaderTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="URL" FontWeight="SemiBold" Foreground="#455A64"/>
                                            </DataTemplate>
                                        </GridViewColumn.HeaderTemplate>
                                        <GridViewColumn.DisplayMemberBinding>
                                            <Binding Path="SiteName"/>
                                        </GridViewColumn.DisplayMemberBinding>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Compte" Width="150">
                                        <GridViewColumn.DisplayMemberBinding>
                                            <Binding Path="Login"/>
                                        </GridViewColumn.DisplayMemberBinding>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Mot de passe" Width="220">
                                        <GridViewColumn.DisplayMemberBinding>
                                            <Binding Path="EncryptedPassword"/>
                                        </GridViewColumn.DisplayMemberBinding>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Date" Width="140">
                                        <GridViewColumn.DisplayMemberBinding>
                                            <Binding Path="CreatedAt"/>
                                        </GridViewColumn.DisplayMemberBinding>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Border>
                </Grid>
            </Grid>

            <!-- Vue des paramètres -->
            <Grid x:Name="SettingsContent"
                  Visibility="{Binding IsSettingsWindowVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                  HorizontalAlignment="Stretch" 
                  VerticalAlignment="Stretch"
                  Panel.ZIndex="1">
                <views:SettingsWindow DataContext="{Binding}"/>
            </Grid>
        </Grid>

        <!-- POP-UP pour ajouter un compte -->
        <Popup IsOpen="{Binding IsPopupOpen}" 
               Placement="Center" 
               AllowsTransparency="True" 
               PopupAnimation="Fade" 
               StaysOpen="False">
            <Border Background="White" 
                    CornerRadius="5" 
                    Padding="20" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Ajouter un nouveau compte" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Margin="0,0,0,10" 
                               Foreground="Black"/>

                    <TextBlock Text="Nom du compte" Foreground="#607D8B" FontSize="12"/>
                    <TextBox Style="{StaticResource ModernTextBox}" 
                             Text="{Binding NewAccount}" 
                             Margin="0,5,0,10"/>

                    <TextBlock Text="Mot de passe" Foreground="#607D8B" FontSize="12"/>
                    <TextBox Style="{StaticResource ModernTextBox}" 
                             Text="{Binding NewPassword}" 
                             Margin="0,5,0,10"/>

                    <TextBlock Text="URL du site" Foreground="#607D8B" FontSize="12"/>
                    <TextBox Style="{StaticResource ModernTextBox}" 
                             Text="{Binding NewURL}" 
                             Margin="0,5,0,10"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Annuler" Command="{Binding ClosePopupCommand}" Style="{StaticResource PrimaryButton}" Background="Gray" Margin="0,0,10,0"/>
                        <Button Content="Ajouter" Command="{Binding AddPasswordCommand}" Style="{StaticResource PrimaryButton}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</Window>
