<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="KryptIt.Views.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:KryptIt.ViewModels"
        xmlns:helpers="clr-namespace:KryptIt.Helpers"
        Title="KryptIt - Register" Height="400" Width="300" Background="#F7F7F7"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:RegistrationViewModel />
    </Window.DataContext>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
            <!-- Logo et titre -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <materialDesign:PackIcon Kind="AccountPlus" Height="24" Width="24" Foreground="Black"/>
                <TextBlock Text="Register" FontWeight="Bold" FontSize="16" Margin="5,0,0,0"/>
            </StackPanel>

            <!-- Message d'inscription -->
            <TextBlock Text="Create a new account" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <!-- Champ Username -->
            <TextBlock Text="Username" FontSize="12" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="25"/>

            <!-- Champ Password -->
            <TextBlock Text="Password" FontSize="12" Margin="0,0,0,5"/>
            <PasswordBox Height="25" 
                         helpers:PasswordBoxHelper.BindPassword="True" 
                         helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Champ Confirm Password -->
            <TextBlock Text="Confirm Password" FontSize="12" Margin="0,0,0,5"/>
            <PasswordBox Height="25" 
                         helpers:PasswordBoxHelper.BindPassword="True" 
                         helpers:PasswordBoxHelper.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Champ Email -->
            <TextBlock Text="Email" FontSize="12" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="25"/>

            <!-- Bouton Register -->
            <Button Content="Register" Command="{Binding RegisterCommand}" Background="Black" Foreground="White" Height="35" Margin="0,10,0,10"/>
        </StackPanel>
    </Grid>
</Window>
